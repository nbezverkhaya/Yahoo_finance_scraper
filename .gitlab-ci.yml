iimage: selenium/standalone-chrome:latest

stages:
  - test

test:
  stage: test
  script:
    - echo "Installing dependencies..."
    - pip3 install --upgrade pip
    - pip3 install -r requirements.txt
    - echo "Running tests..."
    - python -m unittest discover -s tests
